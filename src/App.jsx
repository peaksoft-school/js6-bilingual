import React from "react";

const App = () => {
    return (
        <>
            <div onClick={() => {}} className="res">
                App
            </div>
            <button>item</button>
        </>
    );
};

export default App;
